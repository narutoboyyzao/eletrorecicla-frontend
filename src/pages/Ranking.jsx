import { Award, Medal, UserRound } from 'lucide-react'
import PageHeading from '../components/PageHeading'

export default function Ranking() {
  return <div className="page ranking-page"><PageHeading eyebrow="Classificação por pontos" title="Ranking" description="Compare a participação dos usuários conforme a pontuação acumulada." /><section className="ranking-feature"><div><p className="section-label">Sua posição</p><h2>Endpoint pendente</h2><p>O backend atual não fornece pontuação acumulada nem uma classificação de usuários.</p></div><Medal size={70} /></section><section className="surface ranking-board"><div className="panel-heading"><div><p className="section-label">Classificação geral</p><h2>Usuários com mais pontos</h2></div><span className="api-tag">Endpoint pendente</span></div><div className="ranking-table-head"><span>Posição</span><span>Usuário</span><span>Pontuação</span></div><div className="ranking-waiting">{[1, 2, 3].map(position => <div className="ranking-placeholder" key={position}><span className="position-slot">{position}</span><span className="user-slot"><UserRound size={18} /> Aguardando usuário</span><span className="score-slot">--</span></div>)}</div><div className="ranking-note"><Award size={20} /><p><strong>Classificação sem valores fictícios</strong><br />Esta lista será ordenada quando a API disponibilizar o ranking calculado.</p></div></section></div>
}
