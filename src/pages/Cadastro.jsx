import { useForm } from 'react-hook-form';
import { Link } from 'react-router-dom';
import AuthShell from '../components/AuthShell';
export default function Cadastro(){const {register,handleSubmit,formState:{errors}}=useForm(); return <AuthShell title="Crie sua conta" text="Os campos seguem o caso de uso do TCC. O envio para a API ainda não está ativo."><form onSubmit={handleSubmit(()=>{})} className="auth-form form-grid"><label className="span-2">Nome completo<input {...register('nome',{required:'Informe seu nome.'})}/>{errors.nome&&<small>{errors.nome.message}</small>}</label><label className="span-2">Email<input type="email" {...register('email',{required:'Informe seu email.'})}/>{errors.email&&<small>{errors.email.message}</small>}</label><label>CPF<input placeholder="000.000.000-00" {...register('cpf',{required:'Informe o CPF.'})}/>{errors.cpf&&<small>{errors.cpf.message}</small>}</label><label>Telefone<input placeholder="(00) 00000-0000" {...register('telefone',{required:'Informe o telefone.'})}/>{errors.telefone&&<small>{errors.telefone.message}</small>}</label><label className="span-2">Endereço <span className="field-note">previsto no caso de uso</span><input {...register('endereco',{required:'Informe o endereço.'})}/>{errors.endereco&&<small>{errors.endereco.message}</small>}</label><label className="span-2">Senha<input type="password" {...register('senha',{required:'Informe uma senha.'})}/>{errors.senha&&<small>{errors.senha.message}</small>}</label><button className="auth-submit span-2">Criar conta</button><p className="auth-switch span-2">Já possui conta? <Link to="/login">Entrar</Link></p></form></AuthShell>}
